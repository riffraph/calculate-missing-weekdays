import io
from setuptools import setup, find_packages

setup(
    name='calculate-missing-weekdays',
    version='0.1.0',
    description='',
    author='Raphael Chan',
    url='https://github.com/riffraph/calculate-missing-weekdays',
    packages=find_packages(),
    platforms='any'
)